// GENERATED by extract_round23_guards.py -- edit that, not this.
//
// Sheet geometry for the two city guards. Every sheet is indexed the way the
// player's own art is (see playerAnim.js): columns are frames within a
// direction, rows are directions in PLAYER_DIR_ORDER, so
//   frame = dirRow(facing) * framesPerDir + step
// works for all three states with no per-state special case.
//
// `foot` is the MEASURED lowest opaque row across every frame in that sheet,
// which is where the guard's feet actually are. It differs per state because
// the three states ship at different cell sizes with the character sitting at
// different heights inside them; sharing one origin would make a guard hop
// off the ground the instant they started running.
export const GUARD_ART = {
  "guardM": {
    "idle": {
      "cell": 64,
      "framesPerDir": 1,
      "foot": 62
    },
    "run": {
      "cell": 84,
      "framesPerDir": 8,
      "foot": 75
    },
    "attack": {
      "cell": 84,
      "framesPerDir": 17,
      "foot": 74
    }
  },
  "guardF": {
    "idle": {
      "cell": 64,
      "framesPerDir": 1,
      "foot": 62
    },
    "run": {
      "cell": 88,
      "framesPerDir": 8,
      "foot": 78
    },
    "attack": {
      "cell": 88,
      "framesPerDir": 17,
      "foot": 76
    }
  }
};
