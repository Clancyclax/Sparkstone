// GENERATED by extract_round23_guards.py -- edit that, not this, EXCEPT for
// the round-197 entry at the bottom, which is measured by
// tools/sheet_round196_npcs.py from a delivery that arrived as an NPC rather
// than as a guard. Both sets of numbers mean the same thing.
//
// Sheet geometry for the two city guards. Every sheet is indexed the way the
// player's own art is (see playerAnim.js): columns are frames within a
// direction, rows are directions in PLAYER_DIR_ORDER, so
//   frame = dirRow(facing) * framesPerDir + step
// works for all three states with no per-state special case.
//
// `foot` is the MEASURED lowest opaque row across every frame in that sheet,
// which is where the guard's feet actually are. It differs per state because
// the three states ship at different cell sizes with the character sitting at
// different heights inside them; sharing one origin would make a guard hop
// off the ground the instant they started running.
export const GUARD_ART = {
  "guardM": {
    "idle": {
      "cell": 64,
      "framesPerDir": 1,
      "foot": 62
    },
    "run": {
      "cell": 84,
      "framesPerDir": 8,
      "foot": 75
    },
    "attack": {
      "cell": 84,
      "framesPerDir": 17,
      "foot": 74
    }
  },
  // ROUND 197 -- THE CITY GUARD, who arrived in round 196's fifteen with a
  // run cycle and two attacks and nowhere to stand. His sheets are the NPC
  // ones under the names this manifest's loader expects; `framesPerDir` is 13
  // rather than the other two's 17 because that is the length of the swing he
  // was drawn with, and the loader reads the number rather than assuming it.
  "guardCity": {
    "idle": { "cell": 64, "framesPerDir": 1, "foot": 63 },
    "run": { "cell": 92, "framesPerDir": 8, "foot": 78 },
    "attack": { "cell": 92, "framesPerDir": 13, "foot": 77 }
  },
  "guardF": {
    "idle": {
      "cell": 64,
      "framesPerDir": 1,
      "foot": 62
    },
    "run": {
      "cell": 88,
      "framesPerDir": 8,
      "foot": 78
    },
    "attack": {
      "cell": 88,
      "framesPerDir": 17,
      "foot": 76
    }
  }
};
